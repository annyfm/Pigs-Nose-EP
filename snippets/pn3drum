# -*- mode: snippet -*-
# name: pn3drum
# group: pigsnose
# key: pn3drum
# expand-env: (yas-indent-line fixed)
# --
-- @pn3drum
d'drum $ cf $
  {- -- (1)
  overlay (
    -- every 4 (density 3) $ -- (5)
    s "${4:house}*2"
    ) $ -- -}
  -- every 4 (density 2 . brak) $ -- (2)
  density (8/3) $
  s "${1:sn:0} ${2:hh} $2" -- (3) "$1:0 $2 ${3:rm}"
  -- # n "[0 1]/5" -- (4)
